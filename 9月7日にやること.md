# 9月7日にやること

前日（9/6）までの状態: MVP は完成し、納品物チェックリスト 8 項目をすべて手動確認済み。コードは整理・レビュー済みで GitHub に push 済み（作業ツリーは空）。
GitHub: https://github.com/atsusagi111-dot/knowledge-rag-slack

## A. 動作確認（15 分）

1. VS Code で `模擬案件7` を開き、ターミナルで `npm run bot` を実行して `⚡ Slack ボット起動` が出ることを確認
   - 9/6 深夜にコードを大きく整理したので、起動後に Slack の DM で 1 問（例: `クラウド移行でインフラ運用コストはどれだけ下がりましたか？`）を送り、出典にファイル名と `p.1` が付き、末尾が `gpt-5-mini / 〇〇〇〇 ms`（10000 未満）であることを確認
2. 2 つ目のアカウント（人事のみ）からも同じ質問を送り、「該当なし」になることを確認（権限制御が整理後も効いている証拠）
3. 確認が済んだらボットは `Ctrl` + `C` で止めてよい

## B. GitHub の確認（10 分）

1. リポジトリページで README を読み、直したい表現があれば Claude Code に伝える
2. Actions タブで、`ingest` が「毎日 02:00 JST」に自動実行されているか見る（9/7 02:00 以降に 1 件増えているはず。Scheduled と表示される）
3. https://github.com/settings/emails で「Keep my email addresses private」にチェック（まだなら）

## C. 決めること（Claude Code に伝える）

1. 次のフェーズをどうするか。候補:
   - 一区切りにする（ポートフォリオとして完成）
   - Phase 2 に着手する。着手するならどれからか
     - HNSW インデックス（5,000 件規模の検索速度）
     - Batch API 対応（初回 Embedding 費用を半額に）
     - BM25 ハイブリッド検索（固有名詞・型番に強くする）
     - SharePoint 連携の設計（Microsoft 365 は未契約なので設計のみ）
2. ポートフォリオ用のスクリーンショットを `docs/screenshots/` に置くか（Slack の回答、Actions のログ、Supabase の search_logs）。置けば README から参照する

## D. 任意（時間があれば）

- `docs/governance-for-client.md` を読み、クライアントに渡す文面として違和感がないか確認
- `docs/plan.md` の 14 章「実装後の更新」を読み、計画と実装の差分の説明に納得できるか確認
- 監査ログを Supabase の Table Editor（`search_logs`）で眺め、9/6 の質問がすべて記録されていることを再確認

## 自動で動くもの（何もしなくてよい）

| いつ | 何が | 確認場所 |
|---|---|---|
| 毎日 02:00 JST | 差分取り込み（Supabase の一時停止防止も兼ねる） | GitHub → Actions → ingest |
| 毎週月曜 02:30 JST | 失敗ファイルの再試行 | GitHub → Actions → weekly-retry |
| 10 月 1 日 09:00 JST | 9 月分の監査レポートが Slack DM で届く | 本人と 2 つ目のアカウントの DM |

注意: リポジトリに 60 日間コミットが無いと GitHub の定期実行は止まる。11 月上旬までに 1 回はコミットか手動 Run をする。

## 9/6 に片付けたこと（参考）

- 根本にあった `case7-*` の 8 ファイルは `samples/` と `data/docs/` の複製だったので削除。元の質問 CSV は `eval/questions-original.csv` に移動（Q9 を差し替えた記録のため）
- 36 問版の手動確認リストは 15 問版（`docs/manual-check-15.md`）に統一したので削除
- 作業用の git worktree（`.claude/worktrees/mvp-scaffold`）は main に取り込み済みのため削除
